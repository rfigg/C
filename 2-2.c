#include <stdio.h>

/* Write a loop equivalent to theirs without using && or ||
*/

int main() {
    for (i=0; i<lim-1; ++i) {
        if ((c=getchar()) == EOF)
            break;
        else if (c == '\n')
            break
        else
            s[i] = c;
    }
    return 0;
}

